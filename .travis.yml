os: linux
dist: trusty
language: node_js
node_js: latest

branches:
  only:
  - master

install:
  - npm install

script:
  - npm run package

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master
